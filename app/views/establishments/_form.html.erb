<%= form_with(model: establishment, local: true) do |form| %>
  <% if establishment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(establishment.errors.count, "error") %> prohibited this establishment from being saved:</h2>

      <ul>
      <% establishment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :establishment_user_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :establishment_name %>
  </div>

  <div class="field">
    <%= form.label :link_name %>
    <%= form.text_field :link_name, id: :establishment_link_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
